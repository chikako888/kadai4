name: GitHub Actions Shell Script Test

on: [push]

jobs:
  test-execution:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Run test script
        run: |
          chmod +x gcd.sh test.sh
          ./test.sh
